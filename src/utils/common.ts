export const getLevelColor = (level: number) => {
    if (level === 1) return 'text-green-300'
    if (level === 2) return 'text-green-300'
    if (level === 3) return 'text-green-500'
    if (level === 4) return 'text-yellow-500'
    if (level === 5) return 'text-red-500'
}
